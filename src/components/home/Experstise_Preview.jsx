import React from 'react';
import { ExpertisePreview } from './ExpertisePreview';

// Legacy/typo component kept to match the provided structure.
// Prefer importing `ExpertisePreview`.
export const Experstise_Preview = (props) => {
	return <ExpertisePreview {...props} />;
};

export default Experstise_Preview;

